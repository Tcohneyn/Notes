# **🎯 SetByCaller：让 Ability 控制伤害（本段字幕大纲）**

------

## **1. 当前问题：Gameplay Effect 内部硬编码伤害值**

- G_Damage 中 Scalable Float 的数值是硬编码的 25。
- 希望在创建 Gameplay Effect Spec 或应用时自定义伤害值。
- 理想：由 **Ability 决定伤害值**，而不是 GE 本身。

------

## **2. 伤害值应来自 Ability，而不是 Effect**

- Ability 本身可以拥有基础伤害。
- Projectile Spell 会创建 GE Spec 并把 handle 传入 Projectile。
- Spec Handle 中应包含“该次攻击的伤害值”。
- 因此需要一个机制：**Ability → 指定 GE 的伤害值**。

------

## **3. 第四种 Magnitude 方式：SetByCaller**

- 当前用的是 Scalable Float。
- SetByCaller 是第 4 种方式：
  - 数值由 **创建 Spec 的代码或蓝图直接设置**。
- SetByCaller 使用**键值对（GameplayTag → 数值）**。

------

## **4. 实现步骤：创建 Damage GameplayTag**

1. 在 AuraGameplayTags 单例下创建新 Tag：`Damage`。
2. 在 CPP 中声明 Native Gameplay Tag（复制最后一个 input tag 进行修改）。
3. 名称和字符串都使用 `"Damage"`。

------

## **5. 在 Projectile Spell 中设置 SetByCaller 值**

- 引入 gameplay tags 头文件。
- 获取标签实例：`FAuraGameplayTags GameplayTags = FAuraGameplayTags::Get();`
- 使用 AbilitySystemBlueprintLibrary：
  - 调用 `AssignTagSetByCallerMagnitude()`。
  - 传入：
    - SpecHandle
    - Key: GameplayTags.Damage
    - Value: 50（示例，暂时硬编码）

------

## **6. 接下来修改 Gameplay Effect 使用 SetByCaller**

1. 打开 GE_Damage。
2. 将 Magnitude Type 从 Scalable Float 改为 **SetByCaller**。
3. 在 DataTag 中选择 `Damage`。
4. Modifier 会从 Spec 中读取 50。

------

## **7. 测试结果**

- 伤害从之前的 25 增加到 50。
- 说明 SetByCaller 成功工作。

------

## **8. 当前硬编码问题与下一步**

- 虽然现在 50 是硬编码，但已实现：
  - **Ability → 控制伤害数值**。
- 下一步：
  - 在 AuraGameplayAbility 中加入 “BaseDamage” 成员。
  - 以后根据 Ability 等级来生成伤害。

