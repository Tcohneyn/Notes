以下是这段视频内容的标题大纲式详细中文总结：

### 一、 视觉效果微调（回顾与更新）

- **颜色与材质调整**：
  - 调暗了UI背景色，使其视觉感受更深沉。
  - 修改了流动UI背景（`flowing UI BG`）的颜色参数。
  - 调整了纹理坐标（Texture Coordinate）的 U 和 V 平铺值（Tiling），均设为 **2.2**，以获得更细腻的流动纹理。
- **参数一致性**：展示了颜色选择的具体数值，确保背景和滚动条（`flowing scroll`）风格统一。

### 二、 实现技能菜单的动态开启

- **从 Overlay 移除静态控件**：将之前硬编码在 Overlay 中的技能菜单删除，改为通过按钮动态生成。
- **添加技能按钮（Spells Button）**：
  - 在 Overlay 控件右侧添加一个新的“Wide Button”。
  - 设置文本为全大写的“SPELLS”，并与属性按钮（Attributes）对齐。
- **编写开启逻辑**：
  - 在 `Event Construct` 中绑定按钮点击事件。
  - 调用 `Create Widget` 动态创建 `Spell Menu`。
  - 点击后调用 `Set Is Enabled` 禁用按钮，防止重复开启。

### 三、 窗口位置计算与视口适配

- **右侧对齐数学计算**：
  - 获取视口大小（`Get Viewport Size`）。
  - 获取技能菜单尺寸框（Size Box）的宽度覆盖值。
  - **计算公式**：$X$ 轴位置 = 视口宽度 - 菜单宽度 - 间距（25像素）。
  - $Y$ 轴位置保持与属性菜单一致（设为固定的 25 像素）。
- **参数化间距**：将 25 像素的硬编码数值提升为变量 `Menu Padding`，方便统一调整全局菜单边距。

### 四、 关闭按钮功能与事件派发

- **实现关闭逻辑**：
  - 在 `Spell Menu` 内部，为关闭按钮绑定点击事件，调用 `Remove from Parent`。
- **设置事件分发器（Event Dispatcher）**：
  - 创建 `Spell Menu Closed` 事件分发器。
  - 在 `Event Destruct`（控件销毁）时发出广播。
- **恢复开启按钮**：在 Overlay 控件中监听该广播，当菜单关闭时，重新将“SPELLS”按钮设为启用状态。

### 五、 交互逻辑优化：禁用角色移动

- **输入模式切换**：
  - 引入两个布尔变量：`Attribute Menu Open` 和 `Spell Menu Open`。
  - 当任一菜单开启时，调用 `Set Input Mode UI Only`，防止点击菜单时角色在背景中跑动或误攻击。
- **条件判定恢复**：
  - 当关闭一个菜单时，检查另一个菜单是否仍处于开启状态。
  - **只有当两个菜单都关闭时**，才将输入模式切回 `Set Input Mode Game and UI`，恢复角色移动。

### 六、 蓝图组织与代码重构（Clean Code）

- **函数封装**：为了保持 Overlay 蓝图整洁，将复杂逻辑塌陷（Collapse）为多个函数：
  - `Create Attribute Menu` / `Create Spell Menu`
  - `Attribute Menu Closed` / `Spell Menu Closed`
  - `Input Mode UI Only`
- **初始化逻辑重构**：
  - 封装 `Set Widget Controllers`。
  - 封装消息处理逻辑为 `Handle Message Widget`。
  - 封装升级逻辑为 `Handle Level Up Message`。
- **排版技巧**：
  - 使用 `Sequence` 节点组织流程。
  - 利用 `Ctrl` 键快速移动连线。
  - 使用注释框（Comment）对逻辑块进行颜色编码和分类，提升可读性。

### 下一步预告

- 目前菜单已实现视觉展示、位置适配及交互锁定，下一步将开始编写**技能菜单的实际功能逻辑**。