这段视频主要讲解了如何通过引入 **Gameplay Tags** 和 **随从计数（Minion Count）** 机制，来解决萨满召唤过滥的问题。以下是详细的内容总结：

### 一、 问题诊断：召唤过载与视觉粗糙 [00:00 - 00:40]

- **召唤失控**：萨满目前会无节制地生成随从，导致场面混乱。
- **视觉缺陷**：敌人只是生硬地“蹦”出来，缺乏平滑的视觉过渡（后续将优化）。
- **解决方案**：引入一个随从计数的阈值，只有当随从数量低于上限时才允许召唤。

### 二、 区分技能：创建召唤标签 [00:40 - 02:12]

- **现有问题**：召唤技能目前使用的是 `Abilities.Attack` 标签，这导致 AI 无法区分“普通攻击”与“召唤”。
- **新增标签**：在 C++ 代码中新增 `Abilities.Summon` 标签。
  - 这样做的目的是让萨满能根据当前随从数量，在“火球攻击”和“召唤随从”之间进行智能选择。

### 三、 架构设计：引入随从计数机制 [02:12 - 04:41]

- **通用化设计**：在 `AuraCharacterBase` 基类中添加 `MinionCount` 变量（int32），以便未来玩家角色也能拥有宠物或随从。
- **接口化访问**：在 `CombatInterface` 战斗接口中定义 **`GetMinionCount`** 函数。
  - **优点**：AI 或其他系统无需进行复杂的类类型转换（Cast），只需通过接口即可获取任意角色的随从数量。
- **C++ 实现**：完成接口函数的 override，直接返回角色的 `MinionCount` 属性。

### 四、 AI 行为逻辑分析 [04:41 - 07:07]

- **技能解耦**：在蓝图中将萨满的召唤技能标签改为 `Abilities.Summon`。
- **AI 失效原因**：
  - 目前的 `BTTask_Attack`（攻击任务）硬编码为触发 `Abilities.Attack` 标签。
  - 修改标签后，萨满因为找不到“攻击技能”而变得不再召唤。
- **行为树共享瓶颈**：现有的 `BE_BehaviorTree` 是所有敌人共享的，无法直接在其中加入萨满特有的逻辑。

### 五、 后续计划：定制化行为树 [07:07 - 07:34]

- **策略调整**：为萨满（Shaman）量身定制一套专有的行为树。
- **智能决策逻辑**：
  - 如果 `MinionCount` 低于设定值（如 2 个），则优先触发 `Abilities.Summon`。
  - 如果随从已满，则执行普通的攻击逻辑。

------

核心总结：

本节课通过 C++ 属性扩展 和 Gameplay Tags 细分，为萨满构建了“人口上限”的基础架构。这标志着 AI 从简单的循环触发技能，进化到了能够根据实时状态（随从存活数）进行逻辑决策的阶段。