这段视频记录了如何通过为武器创建**动画蓝图（Animation Blueprint）**，动态控制弹弓敌人的武器骨骼，实现弹弓兜（Pouch）随手部动作拉伸的视觉效果。

以下是内容的标题大纲式详细总结：

### 一、 弹弓动态化的必要性

- **现状**：目前敌人的弹弓是静态武器，显得不够真实。
- **目标**：实现弹弓皮筋的动态拉伸，使哥布林在移动或待机时始终握住弹弓兜（Pouch），而不是让其悬空。

### 二、 创建武器动画蓝图 (ABP_Slingshot)

- **基础设置**：为弹弓的骨架资产创建一个新的动画蓝图 `ABP_Slingshot`。
- **状态机构建**：
  - 建立一个简单的状态机 `Main`。
  - 添加 `Slingshot_Idle` 状态，播放弹弓的默认单帧待机动画。
- **蒙太奇支持**：在输出姿态前添加 **Default Slot** 节点，以便后续播放弹弓的发射蒙太奇。

### 三、 获取所有者手部位置

为了让弹弓兜跟随手部，需要实时获取哥布林角色的手部坐标：

- **角色插槽配置**：在哥布林角色的右手上新建一个名为 `RightHandSocket` 的插槽。
- **事件图表（Event Graph）逻辑**：
  - **初始化**：在 `Initialize Animation` 事件中，获取并保存所有者角色（Owner Character）的网格体组件 `OwnerMesh`。
  - **逐帧更新**：在 `Update Animation` 中，通过 `GetSocketTransform` 实时获取 `RightHandSocket` 的 **世界空间（World Space）** 变换信息。
  - **变量存储**：将变换信息存入变量 `HandSocketTransform`。

### 四、 使用 Transform Modify Bone 修改骨骼

- **节点配置**：在动画图表（Anim Graph）中使用 `Transform (Modify) Bone` 节点。
- **参数设置**：
  - **骨骼选择**：选择弹弓的 `Pouch` 骨骼。
  - **变换模式**：将平移（Translation）和旋转（Rotation）模式均设为 **Replace Existing（替换现有值）**。
  - **空间参考**：选择 **World Space**，以匹配之前获取到的世界空间手部坐标。
- **逻辑连接**：将状态机的输出接入此节点。系统会自动处理局部空间与组件空间的转换（Component-to-Local）。

### 五、 效果验证与后续问题

- **初步成效**：在 `BP_GoblinSlingshot` 蓝图中将武器的动画类指定为 `ABP_Slingshot` 后，可以看到弹弓兜已经能够完美跟随角色的右手，实现了“拉满”的视觉效果。
- **待处理问题**：虽然待机状态表现良好，但在攻击时，弹弓兜仍会粘在手上，无法表现出石块射出时皮筋“回弹”的效果。
- **下阶段目标**：设置开关逻辑，在攻击蒙太奇播放时禁用该骨骼修改功能，实现真实的回弹动画。

Would you like me to explain the steps for disabling this bone modification during the attack montage?