这段视频详细演示了如何在虚幻引擎中从零开始创建一个全新的敌人类型——**食尸鬼 (Ghoul)**。内容涵盖了蓝图类的创建、资产设置、混合空间配置以及基础移动参数的调优。

以下是内容的标题大纲式详细中文总结：

### 一、 核心蓝图类与文件夹结构

- **项目组织**：在 `Content/Blueprints/Character` 下创建 `Ghoul` 文件夹，保持工程整洁。
- **创建角色蓝图**：基于 `EnemyBase` 创建子类蓝图，命名为 `BP_Ghoul`。
- **创建动画蓝图**：基于 `ABP_Enemy` 创建子类动画蓝图，并关联食尸鬼专属骨骼（Skeleton），命名为 `ABP_Ghoul`。

### 二、 角色基础属性配置

- **网格体设置**：
  - 分配食尸鬼骨骼网格体。
  - 调整模型位置和旋转，确保其底部与胶囊体对齐。
- **战斗挂载点 (Sockets)**：
  - 关键重构应用：将 `WeaponTipSocketName` 留空。
  - 填写之前在 C++ 中新增的 `LeftHandSocket` 和 `RightHandSocket` 名称，以便系统识别攻击起始位置。
- **受击反馈**：为食尸鬼创建并分配专属的受击蒙太奇 (`AM_HitReact_Ghoul`)。

### 三、 动画系统：混合空间与资产覆盖

- **创建混合空间 (Blend Space)**：
  - 命名为 `BS_IdleWalk`，用于控制食尸鬼从静止到移动的平滑过渡。
  - **配置轴向**：横轴设为 `Speed`，范围 0 到 100。
  - **采样动画**：在 0 处放置 `Idle` 动画，在 100 处放置 `Walk` 动画。
- **动画平滑**：在 `Sample Smoothing` 下将 `Weight Speed` 设为 4，减少动画切换时的生硬感。
- **应用动画蓝图**：在 `BP_Ghoul` 中指定使用 `ABP_Ghoul`，并观察到角色进入怠速动画。

### 四、 AI 与移动参数调优

- **行为树**：确认角色继承了 `EnemyBase` 的基础行为树，使其具备自动追踪玩家的能力。
- **转向优化**：
  - 禁用控制器旋转（Pitch/Yaw/Roll）。
  - 启用 `Use Controller Desired Rotation`。
  - 将 `Rotation Rate` 下调至 150-200，体现其庞大身躯转向时的迟缓感。
- **行走速度**：
  - 将 C++ 变量 `BaseWalkSpeed` 暴露给编辑器。
  - 将速度从默认值下调至 125，使其呈现一种“步履蹒跚、沉重有力”的移动效果。

### 五、 调试与错误处理

- **数组越界修复**：
  - 发现运行时错误：攻击动作数组为空时尝试访问索引 0 会导致报错。
  - 逻辑加固：在蓝图中增加 `Length > 0` 的判断。如果数组为空，则调用 `End Ability` 结束技能，增强系统的鲁棒性。

### 六、 总结与后续

- **当前状态**：食尸鬼已能正常生成、追踪玩家、受击反应并具备基础的移动逻辑。
- **存在问题**：目前没有任何攻击蒙太奇，因此无法对玩家造成伤害。
- **下一步目标**：创建并配置攻击蒙太奇数组，实现左、右手随机交替攻击。