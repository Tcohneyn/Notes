## 🖥️ 实现屏幕分辨率值转换与功能测试

本讲座成功实现了分辨率设置的关键逻辑：自定义字符串转换、动态填充可用选项、排序以及设置默认值，并验证了其在不同窗口模式下的功能。

------

### 1. ⚙️ 实现自定义字符串转换函数

为了解决动态 Getter/Setter 识别的特殊字符串格式问题，需要创建两个私有辅助函数来处理 `FIntPoint`（分辨率结构体）与字符串之间的转换。

#### A. `RezToValueString` (FIntPoint -> FString)

此函数将 `FIntPoint` 结构体转换为动态 Getter/Setter **期望** 的格式：`(X=1920, Y=1080)`。

- **目的：** 用于存储在数据对象内部，作为实际值。
- **实现细节：** 使用 `FString::Printf` 手动构造字符串，确保包含括号、`X=`、`,` 和 `Y=`。

C++

```
FString UListDataObject_String_Resolution::RezToValueString(const FIntPoint& InResolution) const
{
    // 期望格式: (X=1920, Y=1080)
    return FString::Printf(TEXT("(X=%i, Y=%i)"), InResolution.X, InResolution.Y);
}
```

#### B. `RezToDisplayText` (FIntPoint -> FText)

此函数将 `FIntPoint` 转换为用户友好的显示文本格式：`1920x1080`。

- **目的：** 用于在 UI 中向用户展示。
- **实现细节：** 使用 `FString::Printf` 构造 `WxH` 格式，然后转换为 `FText`。

C++

```
FText UListDataObject_String_Resolution::RezToDisplayText(const FIntPoint& InResolution) const
{
    // 显示格式: 1920x1080
    FString DisplayString = FString::Printf(TEXT("%ix%i"), InResolution.X, InResolution.Y);
    return FText::FromString(DisplayString);
}
```

------

### 2. 📋 实现分辨率初始化 (`InitResolutionValues`)

在 `InitResolutionValues()` 函数中，利用上述转换函数填充选项列表。

- **排序：** 在遍历前，对 `AvailableResolutions` 数组进行排序，确保分辨率从低到高排列。

  - **排序逻辑：** 使用 **Lambda 函数**，基于 **`SizeSquared`**（$\text{X}^2 + \text{Y}^2$）比较分辨率的大小。

  C++

  ```
  AvailableResolutions.Sort([](const FIntPoint& A, const FIntPoint& B) {
      return A.SizeSquared() < B.SizeSquared();
  });
  ```

- **填充选项：** 遍历排序后的数组，使用自定义的转换函数将每个分辨率作为选项添加到列表中。

  - `AddDynamicOption(RezToValueString(Resolution), RezToDisplayText(Resolution))`

- **设置默认值：** 将数组中的 **最大分辨率**（即排序后的最后一个元素）设置为默认值。

  - **存储最大值：** 将最大分辨率的 **Value String** 存储在成员变量 `MaximumAllowedResolution` 中，以便后续使用。
  - **设置默认值：** `SetDefaultValueFromString(MaximumAllowedResolution);`

------

### 3. 🔍 处理初始化时的显示问题

在 `OnDataObjectInitialized()` 函数中，需要确保即使当前分辨率不是可用列表中的标准项（例如在编辑器中运行时），UI 也能正确显示当前值。

- **逻辑：** 尝试使用 `CurrentStringValue` 查找并设置 `DisplayText`。如果失败（即当前分辨率不是标准选项），则从 **`GameUserSettings`** 中获取当前实际分辨率的 `FIntPoint`，并使用 **`RezToDisplayText`** 辅助函数手动生成并设置当前的显示文本。

------

### 4. ✅ 打包构建测试

重新打包项目并运行，验证分辨率设置的功能：

- **Windowed 模式：** 切换分辨率时，游戏窗口大小会随之调整，功能正常。
- **Fullscreen 模式：** 切换分辨率后，屏幕内容会以新的分辨率显示，功能正常。
- **Borderless Window 模式：** **分辨率设置被锁定**（不可编辑），因为该模式会自动使用系统的最大分辨率，这是预期的行为。

**结论：** 屏幕分辨率设置已成功实现，且在各种窗口模式下表现符合预期。

------

> ⏭️ **下一步：** 教程将处理 **编辑条件（Edit Condition）**，以禁用在某些窗口模式（如无边框窗口）下不应更改的设置。